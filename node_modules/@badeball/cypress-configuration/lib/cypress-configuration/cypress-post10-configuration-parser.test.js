"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const strict_1 = __importDefault(require("assert/strict"));
const cypress_post10_configuration_parser_1 = require("./cypress-post10-configuration-parser");
let examples = 1;
function example(source, expected) {
    it(`example #${examples++}`, () => {
        strict_1.default.deepEqual((0, cypress_post10_configuration_parser_1.parsePost10Configuration)(source), expected);
    });
}
describe("parsePost10Configuration()", () => {
    example("module.exports = { e2e: { specPattern: 'foo/bar' } };", {
        specPattern: "foo/bar",
    });
    example("module.exports = { e2e: { specPattern: ['foo/bar'] } };", {
        specPattern: ["foo/bar"],
    });
    example("module.exports = defineConfig({ e2e: { specPattern: 'foo/bar' } });", {
        specPattern: "foo/bar",
    });
    example("module.exports = defineConfig({ e2e: { specPattern: ['foo/bar'] } });", {
        specPattern: ["foo/bar"],
    });
    example("export default { e2e: { specPattern: 'foo/bar' } };", {
        specPattern: "foo/bar",
    });
    example("export default { e2e: { specPattern: ['foo/bar'] } };", {
        specPattern: ["foo/bar"],
    });
    example("export default defineConfig({ e2e: { specPattern: 'foo/bar' } });", {
        specPattern: "foo/bar",
    });
    example("export default defineConfig({ e2e: { specPattern: ['foo/bar'] } });", {
        specPattern: ["foo/bar"],
    });
});
